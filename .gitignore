# Python 虚拟环境
.venv/
venv/
env/

# 环境变量（密钥）
.env

# Python 缓存
__pycache__/
*.pyc
*.pyo

# IDE 配置（可选）
.idea/
.vscode/

# Claude Code 本地缓存与本地开发规范（仅本地使用，不随仓库分发）
.claude/
CLAUDE.md
.workbuddy/

# 运行时生成的资料缓存
materials/
reports/
knowledge/
roadmaps/
learner/

# RAG 向量索引（运行时生成）
.chroma/

# 学习会话状态（运行时生成）
.leamsessions/
.graph

# 内部开发文档与脚本：docs/ 除 internal/ 外随仓库公开（工程记录），
# 私有内容一律放 docs/internal/；scripts/ 是内部工具脚本，不随仓库分发
docs/internal/
scripts/

# Python 构建产物（pip install -e . 生成的 egg-info / build / dist）
*.egg-info/
build/
dist/

